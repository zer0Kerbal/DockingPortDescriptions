// Changelog.cfg v1.0.1.0
// Docking Port Descriptions
// created: 2019 09 17
// updated: 2020 03 03

KERBALCHANGELOG
{
	showChangelog = True
	modName = Docking Port Descriptions
	VERSION
	{
		version = 1.0.1.0
		change = Extreme Flexibility
		change = released for KSP 1.8.1
		change = added Flexible Docking ports
		change = added kerbodynePlus 'Extreme' size
		change = merged two gender patches into one patch
		change = moved into its own permanent GitHub repo
		change = updated to automated build process
		change = folder structure and install location remain the same
	}
	VERSION
	{
		version = 1.0.0.2
		change = Near Future Construction
		change = added :NEEDS to patches
		change = now supports size4, linear1, linear2, spinal, and octo from Near Future Construction
		change = rounded up the herd of missing colons (:) and put them where they belong
	}
	VERSION
	{
		version = 1.0.0.1
		change = >>-- Release --<<
		change = >>-- SpaceDock --<<
		change = >>-- CKAN --<<
		change = >>-- Curseforge --<<
		change = moved to KGEx/
		change = Gender working
		change = adjusted MM timing - needs one :FOR[] before :LAST[] will work, else crashes MM
	}
	VERSION
	{
		version = 1.0.0.0
		change = >>-- creation by zer0Kerbal --<<
	}
}

// CC BY-NC-SA-4.0
// zer0Kerbal